{
  "dependencies": {
    "@types/node": "10.5.5",
    "@types/request-promise-native": "^1.0.15",
    "@wireapp/bot-api": "0.1.0-beta.5",
    "cross-env": "5.2.0",
    "dotenv": "6.0.0",
    "request": "2.87.0",
    "request-promise-native": "1.0.5"
  },
  "devDependencies": {
    "fs-extra": "7.0.0",
    "prettier": "1.14.0",
    "rimraf": "2.6.2",
    "typescript": "3.0.1"
  },
  "license": "GPL-3.0",
  "main": "dist/index.js",
  "name": "wire-web-packages-bot",
  "private": true,
  "repository": "https://github.com/ffflorian/wire-web-packages-bot.git",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "dist": "yarn clean && yarn build",
    "prettier": "prettier --single-quote --write src/**/*.ts",
    "start": "cross-env NODE_DEBUG=@wireapp/* node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "version": "1.0.0"
}
