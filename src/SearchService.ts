class SearchService  {
  static async searchBower(query: string): Promise<string> {
    return 'No results.';
  }

  static async searchNpm(query: string): Promise<string> {
    return 'No results.';
  }

  static async searchCrates(query: string): Promise<string> {
    return 'No results.';
  }

  static async searchTypes(query: string): Promise<string> {
    return 'No results.';
  }
}

export { SearchService };
